# Configuración de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/carkingdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=carking_user
spring.datasource.password=MiPassSeguro123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Puerto de ejecución (mantenido del original)
server.port=8080

# Credenciales por defecto para desarrollo (mantenidas del original)
spring.security.user.name=admin
spring.security.user.password=admin